class: CmsUser
table: cms_users
fields:
  email: String
  name: String?
  role: String, default='viewer'
  createdAt: DateTime?, default=now
indexes:
  cms_users_email_idx:
    fields: email
    unique: true
