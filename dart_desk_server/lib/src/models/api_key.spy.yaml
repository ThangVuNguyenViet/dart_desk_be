class: ApiKey
table: api_keys
fields:
  id: UuidValue, defaultModel=random
  projectId: UuidValue, relation(parent=projects, onDelete=Cascade)
  name: String
  tokenHash: String
  tokenPrefix: String
  tokenSuffix: String
  role: String
  createdByUserId: UuidValue?, relation(parent=users, onDelete=SetNull)
  lastUsedAt: DateTime?
  expiresAt: DateTime?
  isActive: bool, default=true
  createdAt: DateTime?, default=now
indexes:
  api_key_project_idx:
    fields: projectId
  api_key_lookup_idx:
    fields: projectId, tokenPrefix, tokenSuffix
    unique: true
  api_key_prefix_suffix_idx:
    fields: tokenPrefix, tokenSuffix
